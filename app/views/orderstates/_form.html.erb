<%= form_for(@orderstate) do |f| %>
  <% if @orderstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orderstate.errors.count, "error") %> prohibited this orderstate from being saved:</h2>

      <ul>
      <% @orderstate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
