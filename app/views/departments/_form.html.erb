<%= form_for(@department) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Назв отдела" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Продают?" %><br>
    <%= f.check_box :saleright %>
  </div>
  <div class="field">
    <%= f.label "Закупают?" %><br>
    <%= f.check_box :purchaserright %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
